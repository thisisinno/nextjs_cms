'use client'; import Link from 'next/link'; import {useCart} from './cart'; import {motion} from 'framer-motion'; import type {Project,Service} from '@/lib/types';
export function Header(){const {items}=useCart();return <header className="sticky top-0 z-40 border-b border-white/10 bg-ink/95 text-white backdrop-blur"><div className="wrap flex h-16 items-center justify-between"><Link href="/" className="font-bold tracking-[.18em]">ATLAS<span className="text-gold">.</span></Link><nav className="hidden gap-5 text-sm md:flex"><Link href="/about">About</Link><Link href="/services">Services</Link><Link href="/projects">Projects</Link><Link href="/contact">Contact</Link></nav><Link href="/contact#enquiry" className="rounded-full bg-white/10 px-3 py-2 text-sm">Enquiry <b className="text-gold">{items.length}</b></Link></div></header>}
export function SectionHeader({eyebrow,title,text}:{eyebrow:string,title:string,text?:string}){return <div className="mb-10 max-w-2xl"><p className="mb-2 text-sm font-bold uppercase tracking-[.2em] text-gold">{eyebrow}</p><h2 className="text-3xl font-bold text-ink md:text-4xl">{title}</h2>{text&&<p className="mt-4 text-slate-600">{text}</p>}</div>}
export function ServiceCard({service}:{service:Service}){const {add}=useCart();return <motion.article whileHover={{y:-7}} className="card"><div className="h-44 bg-ink/10">{service.image&&<img src={service.image} alt="" className="h-full w-full object-cover"/>}</div><div className="p-6"><p className="text-xs font-bold uppercase text-gold">{service.category||'Service'}</p><h3 className="mt-2 text-xl font-bold">{service.title}</h3><p className="mt-2 line-clamp-3 text-sm text-slate-600">{service.short_description}</p><div className="mt-5 flex gap-3"><Link href={`/services/${service.slug}`} className="text-sm font-bold">View details</Link><button onClick={()=>add(service)} className="text-sm font-bold text-gold">Add to enquiry</button></div></div></motion.article>}
export function ProjectCard({project}:{project:Project}){return <motion.article layout whileHover={{scale:1.02}} className="card"><div className="h-52 bg-ink/10">{project.image&&<img src={project.image} alt="" className="h-full w-full object-cover"/>}</div><div className="p-5"><div className="flex justify-between gap-2"><h3 className="font-bold">{project.title}</h3><span className="text-xs uppercase text-gold">{project.category}</span></div><p className="mt-2 text-sm text-slate-600">{project.location||project.status}</p></div></motion.article>}
export function Loader(){return <div className="py-16 text-center"><span className="inline-block h-8 w-8 animate-spin rounded-full border-4 border-gold border-t-transparent"/></div>}
