export async function resizeImage(file:File,maxWidth=1600,maxHeight=1200,quality=.82):Promise<File>{if(!['image/jpeg','image/png','image/webp'].includes(file.type))throw new Error('Use JPG, PNG, or WebP images.');if(file.size>3*1024*1024)throw new Error('Image must be 3MB or smaller.');const bitmap=await createImageBitmap(file);const ratio=Math.min(maxWidth/bitmap.width,maxHeight/bitmap.height,1);const canvas=document.createElement('canvas');canvas.width=bitmap.width*ratio;canvas.height=bitmap.height*ratio;canvas.getContext('2d')!.drawImage(bitmap,0,0,canvas.width,canvas.height);const blob=await new Promise<Blob|null>(r=>canvas.toBlob(r,'image/webp',quality));if(!blob)throw new Error('Unable to process image.');return new File([blob],file.name.replace(/\.[^.]+$/,'.webp'),{type:'image/webp'});}
