'use client'; import {useEffect,useState} from 'react'; import {api} from '@/lib/api'; import {Loader,ProjectCard,SectionHeader} from '@/components/site'; import type {Project} from '@/lib/types'; import {AnimatePresence} from 'framer-motion'; const cats=['all','completed','ongoing','daily','featured'];export default function Projects(){const [p,setP]=useState<Project[]>();const [cat,setCat]=useState('all');useEffect(()=>{api<any>(`/projects/${cat==='all'?'':`?category=${cat}`}`).then(r=>setP(r.results||r))},[cat]);return <main className="section"><div className="wrap"><SectionHeader eyebrow="Portfolio" title="Work that speaks for itself"/><div className="mb-8 flex flex-wrap gap-2">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`rounded-full px-4 py-2 text-sm capitalize ${cat===c?'bg-ink text-white':'bg-white'}`}>{c}</button>)}</div>{!p?<Loader/>:<AnimatePresence><div className="grid gap-6 md:grid-cols-3">{p.map(x=><ProjectCard key={x.id} project={x}/>)}</div></AnimatePresence>}</div></main>}
