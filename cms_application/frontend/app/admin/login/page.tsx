'use client'; import {useRouter} from 'next/navigation'; import {FormEvent,useState} from 'react'; import {api} from '@/lib/api'; export default function Login(){const r=useRouter(),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();try{const d:any=await api('/auth/token/',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});localStorage.setItem('admin-token',d.access);r.push('/admin')}catch(e:any){setError(e.message)}}return <main className="grid min-h-screen place-items-center bg-ink p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-8"><h1 className="text-2xl font-bold">CMS access</h1><p className="mt-2 text-sm text-slate-600">Use a Django staff account.</p><input className="input mt-6" name="username" placeholder="Username"/><input className="input mt-3" name="password" type="password" placeholder="Password"/><button className="btn btn-gold mt-5 w-full">Sign in</button>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}</form></main>}
