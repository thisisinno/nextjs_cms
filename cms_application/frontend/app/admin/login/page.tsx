'use client'; import {useRouter} from 'next/navigation'; import {FormEvent,useState} from 'react'; import {api} from '@/lib/api'; export default function Login(){const r=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');try{const d:any=await api('/auth/token/',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});localStorage.setItem('admin-token',d.access);r.replace('/admin')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <main className="grid min-h-screen place-items-center bg-ink p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-8 shadow-2xl"><p className="font-black tracking-[.2em] text-gold">SCCL</p><h1 className="mt-3 text-2xl font-bold">CMS access</h1><p className="mt-2 text-sm text-slate-600">Sign in with an authorised Django staff account.</p><input required className="input mt-6" name="username" placeholder="Username"/><input required className="input mt-3" name="password" type="password" placeholder="Password"/><button disabled={loading} className="btn btn-gold mt-5 w-full disabled:opacity-50">{loading?'Signing in…':'Sign in'}</button>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}</form></main>}
