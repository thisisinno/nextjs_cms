'use client'; import {useEffect,useState} from 'react'; import {api} from '@/lib/api'; import {Loader,SectionHeader,ServiceCard} from '@/components/site'; import type {Service} from '@/lib/types'; export default function Services(){const [x,setX]=useState<Service[]>();useEffect(()=>{api<any>('/services/').then(r=>setX(r.results||r))},[]);return <main className="section"><div className="wrap"><SectionHeader eyebrow="Services" title="Technical capability, carefully delivered"/>{!x?<Loader/>:<div className="grid gap-6 md:grid-cols-3">{x.map(s=><ServiceCard key={s.id} service={s}/>)}</div>}</div></main>}
